{
  "name": "k-lunch-budget-dashboard",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --host --port 3000 --open",
    "build": "vite build",
    "preview": "vite preview --port 3000 --open"
  },
  "dependencies": {
    "vite": "^5.2.11"
  }
}
